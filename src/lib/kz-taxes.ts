/**
 * Налоги Республики Казахстан для селлеров на 2026 год.
 *
 * ВСЕ ИСТОЧНИКИ — ПЕРВИЧНЫЕ ОФИЦИАЛЬНЫЕ:
 *   - adilet.zan.kz — Информационная система «Әділет», официальный
 *     электронный публикатор законов РК (Минюст);
 *   - kgd.gov.kz   — Комитет государственных доходов МФ РК
 *     (администратор налогов; региональные ДГД дают разъяснения);
 *   - gov.kz       — электронное правительство РК;
 *   - enpf.kz      — Единый накопительный пенсионный фонд (ОПВ/ОПВР).
 *
 * Никаких пересказов с банковских блогов / частных бухгалтерских сайтов.
 *
 * ───────────────────────────────────────────────────────────────────────
 * ОПОРНЫЕ ДОКУМЕНТЫ
 * ───────────────────────────────────────────────────────────────────────
 *
 * НАЛОГОВЫЙ КОДЕКС РК (новый, действует с 01.01.2026)
 *   Кодекс № 214-VIII от 18.07.2025
 *   https://adilet.zan.kz/rus/docs/K2500000214
 *   (старый K1700000120 утратил силу с 01.01.2026)
 *
 * ЗАКОН О РЕСПУБЛИКАНСКОМ БЮДЖЕТЕ НА 2026-2028 годы
 *   https://adilet.zan.kz/rus/docs/Z2500000239
 *   Постановление о реализации:
 *   https://adilet.zan.kz/rus/docs/P2500001074
 *
 * МРП и МЗП на 2026 год — официальная страница ЭП РК:
 *   https://www.gov.kz/article/17157
 *   Закон о МЗП и Госсоц гарантиях:
 *   https://adilet.zan.kz/rus/docs/Z910001100_
 *
 * ОПВ — Правила (Постановление Правительства):
 *   https://adilet.zan.kz/rus/docs/P2300000525
 *
 * СО — ставка установлена Социальным кодексом РК (ст. 244):
 *   https://adilet.zan.kz/rus/docs/K2300000224
 *   (старое Постановление № 683 от 2004 г. о Правилах СО утратило силу
 *    21.08.2020 постановлением № 530 — больше не цитируем)
 *
 * Правила определения ставок для интернет-платформ:
 *   https://adilet.zan.kz/rus/docs/V2400035173
 *
 * ОСМС — Закон:
 *   https://adilet.zan.kz/rus/docs/Z1500000405
 *   ОСМС — Правила (Приказ):
 *   https://adilet.zan.kz/rus/docs/V1700015361
 *
 * ───────────────────────────────────────────────────────────────────────
 * КЛЮЧЕВЫЕ ФАКТЫ 2026
 * ───────────────────────────────────────────────────────────────────────
 *
 * 1. УПРОЩЁНКА (ИП и ТОО) — базовая ставка 4% (была 3% в 2025), ст. 726 НК РК.
 *    Маслихат региона может ПОНИЗИТЬ или ПОВЫСИТЬ ставку на ±50% → диапазон 2-6%.
 *    Фактически на 2026 почти все регионы СНИЗИЛИ: Алматы и Астана 3%, Шымкент 2%,
 *    большинство районов 2-3%. Ставку берём ПО РЕГИОНУ селлера (uproshenkaRate в
 *    calculateTax / SellerSettings.uproshenkaRatePercent); плоские 4% завышают
 *    налог для большинства селлеров.
 *    Лимит дохода: 600 000 МРП (2026 ≈ 2,595 млрд ₸ при МРП 4 325 ₸).
 *    НДС не платится. Социальный налог ТОО на упрощёнке не платит.
 *    «Розничный налог» в 2026 объединён с упрощёнкой.
 *    Источник: ст. 726 Налогового кодекса РК K2500000214 (Адильет):
 *      https://adilet.zan.kz/rus/docs/K2500000214
 *
 * 2. ОУР (общеустановленный режим):
 *    ТОО: КПН 20% с прибыли (банки/игорный 25%, соцсфера 5%/10%, агро 3-6%).
 *    ИП: вместо КПН — ИПН с дохода по ст. 363 п.4 (10% до 230 000 МРП, 15% свыше).
 *    НДС 16% при обороте > 10 000 МРП ≈ 43,25 млн ₸/год (с 2026; был 12% при 20 000 МРП).
 *    Социальный налог: ТОО 6% (ст. 557); ИП фикс. 2 МРП за себя + 1 МРП за работника.
 *    ИПН работников/физлиц (ст. 363 п.1): до 8 500 МРП — 10%, свыше — 15%.
 *      (источник: ст. 363 и 557 НК РК + разъяснение КГД МФ РК).
 *
 * 3. МРП 2026 = 4 325 ₸, МЗП 2026 = 85 000 ₸
 *    (Закон Z2500000239 «О республиканском бюджете на 2026-2028»).
 *
 * 4. НДС с 01.01.2026 — 16% (ранее 12%); порог регистрации 10 000 МРП.
 *    Льготная ставка для медицинских услуг, лекарств и медизделий:
 *    5% с 01.01.2026, 10% с 01.01.2027.
 *    Источник: Налоговый кодекс РК K2500000214 (республиканский, Адильет)
 *      https://adilet.zan.kz/rus/docs/K2500000214
 *    + КГД МФ РК (головной) «Упрощённый порядок возврата НДС с 2026 года»
 *      https://kgd.gov.kz/en/node/159994
 *
 * 5. СО (соц. отчисления) в 2026 = 5% от объекта (минимум 1 МЗП,
 *    максимум 7 МЗП). Ставка 5% действует с 1 января 2025 года
 *    (ранее 3,5% — это переходная норма, НЕ актуальна для 2026).
 *    Источник: Социальный кодекс РК, статья 244 (Адильет, республиканский):
 *      https://adilet.zan.kz/rus/docs/K2300000224
 *    «...устанавливаются в размере 3,5 процента..., с 1 января 2025 года —
 *     5 процентов от объекта исчисления...». Самозанятые по СНР — 1%.
 *
 * ВНИМАНИЕ:
 *   - Здесь — упрощённая модель для калькулятора маржи. Реальный учёт
 *     требует бухгалтерских деталей (вычеты, ОПВ, ВОСМС, СО, ОПВР,
 *     ОСМС за работника), которые селлер считает отдельно.
 *   - Калькулятор показывает ОЦЕНКУ налога с операции, не годовой расчёт.
 *   - Где конкретная статья НК РК не подтверждена через официальный
 *     источник в формате verbatim, в комментарии стоит ссылка на
 *     adilet ID документа целиком (например K2500000214) и пометка
 *     «требует уточнения статьи».
 */

export type TaxRegime =
  | "ip-uproshenka"
  | "ip-osnovnoy"
  | "too-uproshenka"
  | "too-osnovnoy";

export type TaxRegimeInfo = {
  name: string;
  shortName: string;
  rate: string;
  threshold: string;
  description: string;
  vat: string;
  source: string;
};

/** Адильет — Налоговый кодекс РК № 214-VIII от 18.07.2025, действует с 01.01.2026. */
const NK_RK_2026 = "https://adilet.zan.kz/rus/docs/K2500000214";

/**
 * КГД МФ РК (головной сайт) — разъяснение по ИПН и соцналогу с 01.01.2026.
 * Та же статья доступна на региональных ДГД (vko.kgd.gov.kz, дата 12.02.2026)
 * — по политике КГД синхронной перепубликации содержание идентично.
 */
const KGD_IPN_SN_2026 =
  "https://kgd.gov.kz/ru/content/osnovnye-izmeneniya-v-nalogovom-kodekse-rk-s-1-yanvarya-2026-goda-chto-vazhno-znat-o-ipn-i";

/** КГД МФ РК — упрощённый порядок возврата НДС с 2026 (льготная ставка 5%/10% для медицины). */
const KGD_VAT_REFUND_2026 = "https://kgd.gov.kz/en/node/159994";

/** Адильет — Закон «О республиканском бюджете на 2026-2028 годы» Z2500000239 (МРП и МЗП). */
const RB_2026_2028 = "https://adilet.zan.kz/rus/docs/Z2500000239";

/** ЕНПФ — статья «Изменения в пенсионной системе Казахстана в 2026 году» (ОПВ/ОПВР/СО/ВОСМС). */
const ENPF_2026 =
  "https://www.enpf.kz/ru/press-center/magazine/list.php?ELEMENT_ID=282379";

/** Адильет — Правила и сроки исчисления, удержания (взыскания) и перечисления ОПВ (Постановление Правительства P2300000525). */
const RULES_OPV = "https://adilet.zan.kz/rus/docs/P2300000525";

/**
 * Адильет — Социальный кодекс РК (Кодекс № 224-VII от 20.04.2023), статья 244
 * устанавливает ставку социальных отчислений. Старое Постановление № 683 от
 * 2004 г. о Правилах СО утратило силу 21.08.2020 (постановление № 530).
 */
const SOCIAL_CODE_SO = "https://adilet.zan.kz/rus/docs/K2300000224";

/** Адильет — Правила определения ставок для интернет-платформ (V2400035173). */
const RULES_INTERNET_PLATFORMS = "https://adilet.zan.kz/rus/docs/V2400035173";

/** Адильет — Закон «Об обязательном социальном медицинском страховании» Z1500000405. */
const LAW_OSMS = "https://adilet.zan.kz/rus/docs/Z1500000405";

/** Адильет — Правила исчисления и перечисления ВОСМС (Приказ V1700015361). */
const RULES_OSMS = "https://adilet.zan.kz/rus/docs/V1700015361";

export const TAX_REGIME_INFO: Record<TaxRegime, TaxRegimeInfo> = {
  "ip-uproshenka": {
    name: "ИП на упрощёнке",
    shortName: "ИП-упрощёнка",
    rate: "4% с дохода",
    threshold: "до 600 000 МРП в год (~2,6 млрд ₸)",
    description:
      "С 2026 года базовая ставка 4% (была 3%). Розничный налог объединён с упрощёнкой. " +
      "НДС не платится (специальные режимы освобождены от НДС, разъяснение КГД МФ РК). " +
      "Маслихат региона может изменить ставку ±50% (от 2% до 6%).",
    vat: "Не платится",
    // НК РК 2026 (Адильет) — налоговый кодекс целиком
    source: NK_RK_2026,
  },
  "ip-osnovnoy": {
    name: "ИП на ОУР (общий режим)",
    shortName: "ИП-ОУР",
    rate: "10% ИПН с прибыли + 16% НДС с наценки",
    threshold: "НДС обязателен при обороте > 10 000 МРП (~43,25 млн ₸/год)",
    description:
      "ИП на общеустановленном режиме платит ИПН с прибыли (10% до 8 500 МРП " +
      "дохода, 15% свыше), а не КПН. С 2026: НДС 16% при обороте > 10 000 МРП. " +
      "ИПН по двухступенчатой шкале (разъяснение КГД МФ РК).",
    vat: "16% при обороте > 10 000 МРП (с 2026)",
    // Разъяснение КГД МФ РК по ИПН и соцналогу 2026 (двухступенчатая шкала)
    source: KGD_IPN_SN_2026,
  },
  "too-uproshenka": {
    name: "ТОО на упрощёнке",
    shortName: "ТОО-упрощёнка",
    rate: "4% с дохода",
    threshold: "до 600 000 МРП в год (~2,6 млрд ₸)",
    description:
      "Та же ставка 4%, что и ИП на упрощёнке. ТОО на упрощёнке социальный налог не платит. " +
      "НДС не платится (спец. режимы освобождены, разъяснение КГД МФ РК).",
    vat: "Не платится",
    source: NK_RK_2026,
  },
  "too-osnovnoy": {
    name: "ТОО ОУР (общий режим)",
    shortName: "ТОО-ОУР",
    rate: "20% КПН с прибыли + 16% НДС с наценки",
    threshold: "НДС обязателен при обороте > 10 000 МРП (~43,25 млн ₸/год)",
    description:
      "С 2026: КПН 20%, НДС 16% (был 12%), порог НДС снижен с 20 000 до 10 000 МРП. " +
      "КПН платится с прибыли, НДС — с оборота. Социальный налог 6% (общая ставка). " +
      "ИПН по работникам: 10% до 8 500 МРП, 15% свыше.",
    vat: "16% при обороте > 10 000 МРП (с 2026)",
    // Налоговый кодекс РК K2500000214 (республиканский первоисточник, Адильет)
    source: NK_RK_2026,
  },
};

/**
 * МРП на 2026 = 4 325 ₸.
 * Источник: Закон «О республиканском бюджете на 2026-2028 годы»
 * Z2500000239 (Адильет).
 */
export const MRP_2026 = 4325;

/**
 * МЗП на 2026 = 85 000 ₸.
 * Источник: Закон Z2500000239 (Адильет) + ЕНПФ-разъяснение по
 * пенсионным изменениям 2026 (минимальная база ОПВ = 50 МЗП).
 */
export const MZP_2026 = 85_000;

/** Источник МЗП 2026 (для UI / комментариев). */
export const MZP_2026_SOURCE = RB_2026_2028;

/**
 * Социальный налог на ОУР в 2026 = 6%.
 * Источник: разъяснение КГД МФ РК (ВКО) от 12.02.2026
 * «Основные изменения в Налоговом кодексе РК с 1 января 2026 года...»
 *   {@link KGD_IPN_SN_2026}
 * (требует уточнения статьи НК РК 2026 — полный текст статьи в K2500000214.)
 */
export const SOCIAL_TAX_OUR_PERCENT = 6;
export const SOCIAL_TAX_OUR_SOURCE = KGD_IPN_SN_2026;

/**
 * ИПН (Индивидуальный подоходный налог) — двухступенчатая шкала с 01.01.2026.
 * Это порог для ДОХОДОВ ФИЗЛИЦ И РАБОТНИКОВ (зарплата, ГПХ): ст. 363 п.1 НК РК
 * — до 8 500 МРП в год 10%, свыше 15%.
 * Для ИП/КФХ на ОУР действует ДРУГОЙ порог (230 000 МРП) — см.
 * IPN_IP_OUR_THRESHOLD_MRP (ст. 363 п.4).
 * Источник: ст. 363 НК РК K2500000214 + разъяснение КГД МФ РК {@link KGD_IPN_SN_2026}.
 */
export const IPN_RATES = {
  /** До 8 500 МРП — 10%. */
  standard: 0.10,
  /** Свыше 8 500 МРП — 15%. */
  elevated: 0.15,
  /** Порог в МРП (физлица/работники, ст. 363 п.1). */
  thresholdMRP: 8_500,
  source: KGD_IPN_SN_2026,
} as const;

/**
 * Порог прогрессивной шкалы ИПН для ИП/КФХ на общеустановленном режиме —
 * 230 000 МРП в год (ст. 363 п.4 НК РК 2026): до него 10%, свыше — 15%.
 * ОТЛИЧАЕТСЯ от порога для зарплат/физлиц (8 500 МРП, ст. 363 п.1).
 * При МРП 4 325 ₸ это ~994,75 млн ₸ в год — на одну продажу недостижимо,
 * поэтому в расчёте налога с операции для ИП на ОУР берём базовые 10%.
 */
export const IPN_IP_OUR_THRESHOLD_MRP = 230_000;

/**
 * Ставки 2026 (после реформы НК 2026).
 *
 * Источники:
 *   - Упрощёнка 4% — НК РК 2026 K2500000214 (требует уточнения статьи) +
 *     разъяснение КГД МФ РК {@link NK_RK_2026};
 *   - КПН 20% — НК РК 2026 K2500000214 (требует уточнения статьи);
 *   - НДС 16% и порог 10 000 МРП — разъяснение КГД МФ РК {@link NK_RK_2026}.
 */
export const RATES_2026 = {
  /** Упрощёнка — базовая ставка с 2026 (была 3%, стала 4%). */
  uproshenka: 0.04,
  /** Минимальная ставка в регионе после маслихата −50%. */
  uproshenkaMin: 0.02,
  /** Максимальная ставка в регионе после маслихата +50%. */
  uproshenkaMax: 0.06,
  /** КПН на ОУР, % от прибыли. */
  kpn: 0.2,
  /** НДС с 2026 = 16% (был 12% в 2025). */
  vat: 0.16,
  /** Порог обязательной регистрации по НДС, в МРП. */
  vatThresholdMRP: 10_000,
  /**
   * Социальный налог (ОУР) — общая ставка с 2026.
   * НЕ путать с социальными отчислениями (СО): см. SOCIAL_CONTRIBUTIONS_2026.so.
   */
  socialTaxOur: 0.06,
  /**
   * Социальные отчисления (СО) — ставка для ИП/работодателя в 2026 = 5%.
   * Ставка 5% действует с 1 января 2025 года (ранее была 3,5% — переходная
   * норма, для 2026 НЕ актуальна).
   * Источник: Социальный кодекс РК, ст. 244 {@link SOCIAL_CODE_SO} + ЕНПФ {@link ENPF_2026}.
   */
  social: 0.05,
} as const;

/**
 * Ставки НДС с 01.01.2026.
 *
 * Источники:
 *   - стандартная 16% — разъяснение КГД МФ РК {@link NK_RK_2026};
 *   - льготная 5%/10% для медицины — разъяснение КГД МФ РК
 *     «Упрощённый порядок возврата НДС с 2026 года»
 *     {@link KGD_VAT_REFUND_2026}.
 */
export const VAT_RATES_2026 = {
  /** Стандартная ставка с 01.01.2026. */
  standard: 0.16,
  /** Медицинские услуги, лекарства, медизделия — 5% в 2026. */
  medical2026: 0.05,
  /** Те же категории — 10% с 01.01.2027 (поэтапный рост). */
  medical2027: 0.10,
  source: NK_RK_2026,
  medicalSource: KGD_VAT_REFUND_2026,
} as const;

/**
 * Социальные взносы (для ИП за себя и для работодателя с 01.01.2026).
 *
 * Все четыре взноса: ОПВ / СО / ВОСМС / ОПВР.
 *
 * Источники:
 *   - ОПВ: Правила P2300000525 (Адильет) {@link RULES_OPV} + ЕНПФ {@link ENPF_2026};
 *   - СО:  Социальный кодекс РК ст. 244 (Адильет) {@link SOCIAL_CODE_SO} + ЕНПФ {@link ENPF_2026};
 *   - ВОСМС: Закон Z1500000405 + Правила V1700015361 + ЕНПФ {@link ENPF_2026};
 *   - ОПВР (обязательные пенсионные взносы работодателя): ЕНПФ {@link ENPF_2026},
 *     поэтапный рост, в 2026 = 3,5% (был ниже в 2025).
 *
 * Пределы объекта — в МЗП в МЕСЯЦ (2026 МЗП = 85 000 ₸), НЕ в год:
 *   - ОПВ — максимум 50 МЗП/мес (4 250 000 ₸); нижнего предела для работника нет;
 *   - СО  — от 1 до 7 МЗП/мес (85 000 … 595 000 ₸);
 *   - ВОСМС — ИП «за себя» фикс. 1,4 МЗП; за работника удержание 2%;
 *   - ООСМС — максимум 40 МЗП/мес; ОПВР — от 1 до 50 МЗП/мес.
 */
export const SOCIAL_CONTRIBUTIONS_2026 = {
  /**
   * ОПВ — обязательные пенсионные взносы, 10% (ст. 248-249 Соц. кодекса).
   * Объект ограничен СВЕРХУ 50 МЗП в месяц; обязательного нижнего предела
   * для работника нет (можно от фактического дохода).
   */
  opv: {
    rate: 0.10,
    /** Верхний предел объекта, МЗП/месяц. */
    maxBaseInMZP: 50,
    source: RULES_OPV,
    enpfSource: ENPF_2026,
  },
  /**
   * СО — социальные отчисления. В 2026 = 5% от объекта (ст. 244 Соц. кодекса).
   * Ставка 5% действует с 1 января 2025 года. Объект в месяц: минимум 1 МЗП,
   * максимум 7 МЗП. Самозанятые по СНР — 1%.
   */
  so: {
    rate: 0.05,
    /** Нижний предел объекта, МЗП/месяц. */
    minBaseInMZP: 1,
    /** Верхний предел объекта, МЗП/месяц. */
    maxBaseInMZP: 7,
    source: SOCIAL_CODE_SO,
    enpfSource: ENPF_2026,
  },
  /**
   * ВОСМС — взносы на ОСМС. Ставка зависит от того, кто платит:
   *  - ИП «за себя»: 5% от 1,4 МЗП (фиксированный объект);
   *  - за работника: 2% удерживаются ИЗ зарплаты работника
   *    (отдельно работодатель платит ООСМС 3% за свой счёт, см. oosms).
   * Источник: Закон об ОСМС Z1500000405 (ст. 27, 28) + Правила V1700015361.
   */
  vosms: {
    /** ИП «за себя». */
    selfRate: 0.05,
    /** Объект для ИП «за себя» — 1,4 МЗП. */
    selfBaseInMZP: 1.4,
    /** Удержание из зарплаты работника. */
    withheldRate: 0.02,
    source: LAW_OSMS,
    rulesSource: RULES_OSMS,
    enpfSource: ENPF_2026,
  },
  /**
   * ООСМС — отчисления работодателя на ОСМС за свой счёт (за работников), 3%.
   * Объект ограничен сверху 40 МЗП/мес. НЕ путать с ВОСМС-удержанием 2%.
   */
  oosms: {
    rate: 0.03,
    /** Верхний предел объекта, МЗП/месяц. */
    maxBaseInMZP: 40,
    source: LAW_OSMS,
    rulesSource: RULES_OSMS,
  },
  /**
   * ОПВР — обязательные пенсионные взносы работодателя (ст. 251 Соц. кодекса).
   * В 2026 = 3,5% (вырос с 2,5% в 2025), поэтапный рост до 5% к 2028.
   * Объект в месяц: минимум 1 МЗП, максимум 50 МЗП.
   */
  opvr: {
    rate: 0.035,
    /** Нижний предел объекта, МЗП/месяц. */
    minBaseInMZP: 1,
    /** Верхний предел объекта, МЗП/месяц. */
    maxBaseInMZP: 50,
    source: ENPF_2026,
  },
  /** Правила определения ставок для интернет-платформ (Адильет V2400035173). */
  internetPlatformsSource: RULES_INTERNET_PLATFORMS,
} as const;

/** Лимит дохода на упрощёнке, в МРП. */
export const UPROSHENKA_INCOME_LIMIT_MRP = 600_000;

/** Источник лимита упрощёнки (Налоговый кодекс РК K2500000214, Адильет). */
export const UPROSHENKA_INCOME_LIMIT_SOURCE = NK_RK_2026;

export type TaxCalculationInput = {
  /** Оборот (выручка) с этой операции, ₸ */
  revenue: number;
  /** Прибыль до налога, ₸ (используется для КПН/ИПН) */
  profitBeforeTax: number;
  /** Налоговый режим */
  regime: TaxRegime;
  /**
   * Ставка упрощёнки (доля, напр. 0.03 для 3%) с учётом решения маслихата
   * региона. Базовая 4% (ст. 726 НК РК), маслихат может изменить её на ±50%
   * (диапазон 2-6%). Если не передана — берётся базовая RATES_2026.uproshenka.
   * Применяется только к режимам упрощёнки; для ОУР игнорируется.
   */
  uproshenkaRate?: number;
  /**
   * Себестоимость (закупка), ₸ — нужна для КОРРЕКТНОГО НДС на ОУР: НДС платится
   * с НАЦЕНКИ (revenue − cost), а не со всей выручки. Если не задана (0) — НДС
   * считается как выходной с цены без зачёта входного (консервативно, но всё
   * равно не «16% со всей выручки», а 16/116 от цены).
   */
  cost?: number;
  /**
   * Плательщик ли НДС (оборот > 10 000 МРП ≈ 43,25 млн ₸/год). Только для ОУР.
   * По умолчанию true (на ОУР обычно идут как раз из-за НДС-порога). false → НДС 0.
   */
  isVatPayer?: boolean;
  /**
   * Ставка НДС (доля) для ОУР с учётом льготных категорий (медицина/лекарства 5%
   * в 2026). По умолчанию стандартная RATES_2026.vat (16%). Применяется к НДС с наценки.
   */
  vatRate?: number;
};

export type TaxCalculationResult = {
  /** Сумма налога с операции, ₸ */
  amount: number;
  /** Из чего складывается */
  breakdown: Array<{ label: string; amount: number }>;
  /** Эффективная ставка налога к выручке, % */
  effectiveRatePercent: number;
};

/**
 * Считает налог с операции для калькулятора маржи.
 *
 * ВНИМАНИЕ: упрощённая ПО-ОПЕРАЦИИ оценка, не годовой расчёт. Не заменяет
 * бухгалтера — финальные суммы сверять с учётом.
 *
 * Логика:
 *   - Упрощёнка (ИП/ТОО): ставка региона с ОБОРОТА, ВСЕГДА (ст. 726 НК РК),
 *     в т.ч. при убытке — на упрощёнке налог с дохода не зависит от прибыли
 *     (A3: иначе у точки безубыточности разрыв и занижение).
 *   - ОУР: налог с прибыли (ИП — ИПН 10%, ТОО — КПН 20%) + НДС.
 *     НДС берётся С НАЦЕНКИ (A2): цены Kaspi розничные, т.е. НДС-включающие,
 *     поэтому к уплате = (revenue − cost) × r/(1+r), а не «16% со всей выручки»
 *     (это завышало налог в разы и делало прибыльный товар «убыточным»).
 */
export function calculateTax(input: TaxCalculationInput): TaxCalculationResult {
  const { revenue, profitBeforeTax, regime } = input;
  const cost =
    typeof input.cost === "number" && isFinite(input.cost) && input.cost > 0 ? input.cost : 0;
  const isVatPayer = input.isVatPayer ?? true;
  const breakdown: Array<{ label: string; amount: number }> = [];
  let amount = 0;

  if (regime === "ip-uproshenka" || regime === "too-uproshenka") {
    // A3: упрощёнка платится с оборота независимо от прибыли (ст. 726 НК РК).
    const rate = resolveUproshenkaRate(input.uproshenkaRate);
    const tax = round2(revenue * rate);
    breakdown.push({ label: "Упрощёнка " + formatRatePercent(rate) + " с оборота", amount: tax });
    amount = tax;
    return {
      amount,
      breakdown,
      effectiveRatePercent: revenue > 0 ? round2((amount / revenue) * 100) : 0,
    };
  }

  if (regime === "ip-osnovnoy" || regime === "too-osnovnoy") {
    // Налог с прибыли: ИП на ОУР — ИПН 10% (ст. 363); ТОО на ОУР — КПН 20%.
    const profitRate = regime === "ip-osnovnoy" ? IPN_RATES.standard : RATES_2026.kpn;
    const profitLabel = regime === "ip-osnovnoy" ? "ИПН 10% с прибыли" : "КПН 20% с прибыли";
    const profitTax = profitBeforeTax > 0 ? round2(profitBeforeTax * profitRate) : 0;
    if (profitTax > 0) breakdown.push({ label: profitLabel, amount: profitTax });

    // A2: НДС с наценки (цены НДС-включающие, входной НДС зачтён). Ставка учитывает
    // льготные категории (медицина 5% в 2026), если передана vatRate.
    let vat = 0;
    if (isVatPayer) {
      const vatRate =
        typeof input.vatRate === "number" && isFinite(input.vatRate) && input.vatRate > 0
          ? input.vatRate
          : RATES_2026.vat;
      vat = vatOnMargin(revenue, cost, vatRate);
      breakdown.push({
        label: `НДС ${formatRatePercent(vatRate)} с наценки (цены с НДС, входной зачтён)`,
        amount: vat,
      });
    }
    amount = round2(profitTax + vat);
    return {
      amount,
      breakdown,
      effectiveRatePercent: revenue > 0 ? round2((amount / revenue) * 100) : 0,
    };
  }

  // На случай если расширят TaxRegime
  return { amount: 0, breakdown: [], effectiveRatePercent: 0 };
}

/**
 * НДС к уплате на ОУР для перепродажи: с добавленной стоимости (наценки).
 * Цены РК розничные, НДС-включающие, поэтому ставка применяется как r/(1+r).
 * При cost=0 (неизвестна) — выходной НДС с цены без зачёта (консервативно).
 * Floored на 0: модель «с операции» не возвращает НДС-кредит.
 */
function vatOnMargin(revenue: number, cost: number, rate: number): number {
  const addedValue = Math.max(0, revenue - cost);
  return round2((addedValue * rate) / (1 + rate));
}

/** Округление до сотых (для денег в ₸ — целых, но оставляем для тестов гибкость). */
function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

/**
 * Нормализует ставку упрощёнки: пустое/невалидное значение → базовая 4%;
 * валидное число клиппируется в юридический диапазон 2-6% (ст. 726 НК РК,
 * маслихат ±50%). Контракт: ставка передаётся ДОЛЕЙ (0.03 = 3%).
 * Используется и в calculateTax, и в store-calc, чтобы расчёты не расходились.
 */
export function resolveUproshenkaRate(rate?: number): number {
  if (typeof rate !== "number" || !isFinite(rate) || rate <= 0) {
    return RATES_2026.uproshenka;
  }
  if (rate < RATES_2026.uproshenkaMin) return RATES_2026.uproshenkaMin;
  if (rate > RATES_2026.uproshenkaMax) return RATES_2026.uproshenkaMax;
  return rate;
}

/** «0.04» → «4%», «0.035» → «3,5%» (запятая как десятичный разделитель RU). */
function formatRatePercent(fraction: number): string {
  const v = fraction * 100;
  const s = Number.isInteger(v) ? String(v) : v.toFixed(1).replace(".", ",");
  return s + "%";
}

/** Список вариантов для UI dropdown. */
export function getTaxRegimeOptions(): Array<{ value: TaxRegime; label: string }> {
  return [
    { value: "ip-uproshenka", label: TAX_REGIME_INFO["ip-uproshenka"].shortName + " (4%)" },
    { value: "ip-osnovnoy", label: TAX_REGIME_INFO["ip-osnovnoy"].shortName + " (ИПН 10% + НДС 16%)" },
    { value: "too-uproshenka", label: TAX_REGIME_INFO["too-uproshenka"].shortName + " (4%)" },
    { value: "too-osnovnoy", label: TAX_REGIME_INFO["too-osnovnoy"].shortName + " (КПН 20% + НДС 16%)" },
  ];
}
